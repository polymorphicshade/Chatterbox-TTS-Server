# TODO

## OpenAI API Support
I want the ability to "chat" (including voice-to-text) with an OpenAI endpoint while using the voice features of this app.

I want a simple chat UI I can use to talk to an LLM, where the responses come in like any other chat app, but it will also auto-play voice audio using the response as the prompt input.

Make this a separate tab - so have a "General" tab for normal flow, and a "Chat" tab for this new feature

In this "Chat" tab, include settings for:
- the endpoint (i.e. URL, model, system message, etc)
- if the responses auto-play or not (enabled by default)
- auto-detect emotion from the response (and context - use an LLM prompt to determine this), or use the voice generation settings, just use what's set in the "General" tab




